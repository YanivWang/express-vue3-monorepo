# 在 base + dev 之上再叠加本文件，在容器内启用 Node Inspector。
# pnpm docker:dev:debug

services:
  rest-api:
    command:
      - sh
      - -c
      - mkdir -p /app/logs && pnpm rest-api:dev:debug 2>&1 | tee -a /app/logs/rest-api-dev.log
