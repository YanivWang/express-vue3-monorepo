# 在 base + dev 之上再叠加本文件，在容器内启用 Node Inspector。
# pnpm docker:dev:debug

services:
  app:
    command:
      - sh
      - -c
      - mkdir -p /app/logs && pnpm dev:debug 2>&1 | tee -a /app/logs/app-dev.log
