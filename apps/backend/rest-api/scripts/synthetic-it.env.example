# -----------------------------------------------------------------------------
# 合成 IT 帖子 / 种子脚本专用环境变量（示例）
#
# 使用方式：
#   在本目录复制：`cp synthetic-it.env.example synthetic-it.env`
#   按需填写；`synthetic-it.env` 与 `synthetic-it.env.local` 已列入 .gitignore，勿提交。
#   `synthetic-it-merge-monorepo-dotenv.ts` 会先读 monorepo 根的 `.env.*`，再读本目录
#   `synthetic-it.env`、`synthetic-it.env.local`，并对下列前缀的键**覆盖**根目录同源键
#   （仅影响运行种子相关脚本时的进程）。
#
# 命令：
#   仓库根：pnpm --filter @vue3-express-monorepo/rest-api synthetic-it:seed
#   或 cd apps/backend/rest-api && pnpm synthetic-it:seed
#   仅写示例类目：`pnpm it:seed-categories`（空表才写入；不写帖子）
#   仅清帖：pnpm synthetic-it:clear（含链式类目种子 + 删 synthetic 帖）
#   仅去重索引：pnpm db:dedupe-indexes
#
# 默认「IT技术」两级类目：**不再**随 API `connectDatabase` 写入；由 `pnpm it:seed-categories`
#   或由合成帖命令链写入。数据来源：scripts/it-category-seed.json
#   — 须与 synthetic-it-data.ts 中各分类 categoryName 一致
# -----------------------------------------------------------------------------

# --- 索引去重（synthetic-it:seed 第一步）---
# DEDUPE_INDEXES_DRY_RUN=1

# --- 调 API（synthetic-it-run）---
# REST_API_BASE=http://127.0.0.1:2026/api
# REST_API_IMPORT_TOKEN=
# REST_API_IMPORT_USERNAME=
# REST_API_IMPORT_PASSWORD=

# --- 与 ensure-super-admin 一致；种子在未设 REST_API_IMPORT_TOKEN 时 POST /login 使用 ---
# ENSURE_SUPER_ADMIN_USERNAME=root
# ENSURE_SUPER_ADMIN_PASSWORD=123456

# --- 静态 / LLM / 配图 ---
# SYNTHETIC_RATE_MS=120
# SYNTHETIC_USE_STATIC_BUNDLE=1
# SYNTHETIC_LLM_API_KEY=
# SYNTHETIC_LLM_BASE_URL=https://api.openai.com/v1
# SYNTHETIC_LLM_MODEL=gpt-4o-mini
# SYNTHETIC_LLM_JSON_OBJECT=0
# SYNTHETIC_LLM_MAX_TOKENS=4096
# SYNTHETIC_LLM_TEMPERATURE=
# SYNTHETIC_FETCH_IMAGES=
# SYNTHETIC_PEXELS_API_KEY=
