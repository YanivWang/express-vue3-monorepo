---
description: Vue 前端应用与 shared 包：技术栈、exports 子路径、样式与 lint
globs: apps/frontend/**/*.{ts,vue,js,mjs}, packages/shared/**/*.ts, apps/frontend/**/*.{css,scss}
alwaysApply: false
---

# Frontend：`apps/frontend/*` 与 `packages/shared`

## 应用

- **PC 门户**：`apps/frontend/pc-portal`（`@express-vue3-monorepo/pc-portal`）；Vite **5173**；Token 键 **`pc_portal_access_token`**
- **PC 管理后台**：`apps/frontend/pc-admin`（`@express-vue3-monorepo/pc-admin`）；Vite **5174**；Token 键 **`pc_admin_access_token`**；网关子路径 **`VITE_ADMIN_BASE=/pc-admin/`**（Compose / 生产）
- 开发与构建以各 app 的 `package.json` `scripts` 为准；两应用的 `typecheck` 均为 **`vue-tsc --noEmit -p tsconfig.app.json`**，`dev` 为 **`vite`**。
- **工作区依赖**：两应用均依赖 **`@express-vue3-monorepo/shared`**；HTTP 经 shared 的 `request-pc` 间接使用 **`@express-vue3-monorepo/request-core`**。
- **pc-admin 权限**：`main.ts` 启动时 `bootstrapSession()`；路由守卫用 `hasStaffEntry`（须任一 `admin.*`）与 `meta.permissions`（`hasAnyPermission`，OR）。详见 `docs/admin-permissions.md`。

## 技术栈（与根 catalog / 各包依赖一致）

- **Vue 3**、**Vite**、**TypeScript**、**Pinia**、**Vue Router**、**Sass**。
- **PC UI**：**Element Plus**、**@vueuse/core**（以 app 的 `dependencies` 为准）。
- **pc-portal** 另依赖 **`@yanivjs/yaniv-editor`**、**Ant Design Vue**（editor peer）、**`@tiptap/*`** 等；见 `docs/pc-portal-yaniv-editor.md`。
- **pc-admin** 另依赖 **nprogress**（路由进度条）。

## `@express-vue3-monorepo/shared`

- **职责**：types、utils（Token 存储）、auth 会话工厂、`request-pc` preset；HTTP 与 `apps/backend/rest-api` 对齐。
- **内部依赖**：`@express-vue3-monorepo/request-core`（见 `packages/shared/package.json`）。
- **从 shared 导入**：只使用 **`package.json` → `exports` 声明的子路径**：
  - `@express-vue3-monorepo/shared/types`
  - `@express-vue3-monorepo/shared/utils`
  - `@express-vue3-monorepo/shared/constants`
  - `@express-vue3-monorepo/shared/auth`
  - `@express-vue3-monorepo/shared/request-pc`
- **HTTP 客户端**：行为与后端 **`{ code, msg, ... }`** 对齐；各 app 通过 **`createAppPcHttp`** 创建实例，勿重复搭 axios。

## 目录与复用

- 跨 app 的 HTTP / 会话逻辑放 **shared**；业务页面、路由、RBAC 视图留各 app。

## 样式与质量

- **Stylelint**：根脚本 `pnpm lint:style`。
- **ESLint**：根目录 **`eslint.config.mjs`**；提交前 **`pnpm verify`**。
- **Shared 类型检查**：`packages/shared` 内 `pnpm typecheck` 为 **`tsc --noEmit`**（纯 TS，无 `.vue`）。

## Vue SFC

- 优先 **Composition API**、`<script setup lang="ts">`，风格与现有页面保持一致。
