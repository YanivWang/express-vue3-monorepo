/**
 * Conventional Commits（与 vue3-monorepo 同类）。
 * 建议 scope：rest-api | pc-portal | pc-admin | repo | deps | docker | frontend | backend
 */
module.exports = {
  extends: ["@commitlint/config-conventional"],
};
