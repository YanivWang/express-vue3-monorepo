# 复制为 .env.development 后按需修改（勿提交本地密钥）。
# JWT_SECRET 需 ≥32 字符且不能是 env.js 中的弱密码列表。
# Docker：pnpm docker:dev（Compose 会自动把 DB_HOST 覆盖为 mysql，见 docker-compose.base.yaml）
# 本机直连 MySQL（非 Compose）：DB_HOST 用 127.0.0.1

APP_ENV=development
NODE_ENV=development

JWT_SECRET=replace_with_random_string_at_least_32_characters_long

# 宿主与 Compose 端口占位（可多栈并行调度）
# MYSQL_HOST_PORT 仅开发栈 docker-compose.dev.yaml 映射 MySQL；生产/测试 Compose 不使用，保留键名便于跨环境对齐
PORT=3000
APP_HOST_PORT=3000
MYSQL_HOST_PORT=3306

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=express_demo_02
DB_USER=app_user
DB_PWD=app_password_change_me

MYSQL_ROOT_PASSWORD=root_password_change_me

# Winston 日志级别（可选，未设置时默认为 info）；见 src/utils/logger.js
LOG_LEVEL=info
