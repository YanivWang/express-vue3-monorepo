{
  "name": "express-demo-01",
  "version": "1.0.0",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/app.js",
    "dev:debug": "nodemon --inspect=0.0.0.0:9229 src/app.js",
    "docker:dev": "docker compose --env-file .env -f docker/docker-compose.dev.yaml up -d --build",
    "docker:dev:debug": "docker compose --env-file .env -f docker/docker-compose.dev.yaml -f docker/docker-compose.debug.yaml up -d --build",
    "docker:prod": "docker compose --env-file .env -f docker/docker-compose.yaml up -d --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "4.22.1",
    "js-yaml": "^4.1.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.3",
    "sequelize": "^6.37.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["bcrypt"]
  }
}
